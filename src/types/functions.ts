/**
 * Types for function buildApiUrl
 *
 * ../utils/buildApiUrl.ts
 * */
export type BuildApiUrlTypes = {
  endpoint: string;
  params?: Record<string, string | number | boolean | null | undefined>;
};
